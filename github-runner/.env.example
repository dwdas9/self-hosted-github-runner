# GitHub Actions Self-Hosted Runner Configuration
# Copy this file to .env and fill in your values

# Required: GitHub Personal Access Token
# Needs 'repo' scope for repository runners or 'admin:org' for organization runners
GITHUB_TOKEN=your_github_token_here

# Option 1: Repository-level runner (format: owner/repository)
GITHUB_REPOSITORY=your-username/your-repository

# Option 2: Organization-level runner (use instead of GITHUB_REPOSITORY)
# GITHUB_OWNER=your-organization

# Optional: Custom runner name
RUNNER_NAME=my-docker-runner

# Optional: Custom labels (comma-separated)
RUNNER_LABELS=docker,ubuntu,self-hosted,custom
